template
	require(from="./file-dropper.scss")
	form.box(ref="uploader" method="post" action="" enctype="multipart/form-data")
		.box__input
			input.box__file(
				type="file" 
				name="files[]" 
				id="file" 
				data-multiple-caption="{count} files selected" 
				multiple)
			label(for="file")
				strong Choose a file
				span  or drag it here to upload.

			button.box__button(type="submit") Upload
		.box__uploading 
			i Uploading 
			.rotate &hellip;
		.box__success 
			.done Done!
		.box__error Error!
		.box__drophere 
			strong Drop Here!